<Window x:Class="Warehouse.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vms="clr-namespace:Warehouse.ViewModels"
        Title="Reports" Height="300" Width="300">
    
    <Window.DataContext>
        <vms:WarehouseViewModel/>
    </Window.DataContext>
    
    
    <StackPanel>
        <Grid DataContext="{Binding NewProduct}">
            <DatePicker SelectedDate="{Binding DayOfPurchase}"/>
        </Grid>
        <DataGrid Name="DataGridReports" ItemsSource="{Binding ProductsReports}" AutoGenerateColumns="False" CanUserResizeColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Buy Price" Binding="{Binding BuyPrice}" Width="*"/>
                <DataGridTextColumn Header="Sell Price" Binding="{Binding SellPrice}" Width="*"/>
                <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor.Name}" Width="*"/>
                <DataGridTextColumn Header="Date Of Purchase" Binding="{Binding DayOfPurchase}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Name="GetReport" Content="Get Report" Command="{Binding ProductReport}"/>
    </StackPanel>
</Window>
